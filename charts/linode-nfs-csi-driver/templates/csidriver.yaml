apiVersion: storage.k8s.io/v1
kind: CSIDriver
metadata:
  name: {{ .Values.driver.name }}
spec:
  attachRequired: {{ eq .Values.accessPolicy.mode "node" }}
  podInfoOnMount: false
  fsGroupPolicy: File
